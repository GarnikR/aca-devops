name: website
run-name: ${{github.actor}}

on: [push]

jobs:
  build_website:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: run a script
      run: ../../../terraform/project.yaml
  
